// `server-only` es un marcador que Next usa para impedir que un módulo acabe
// en el navegador. Fuera de Next no existe, así que en las pruebas se
// sustituye por este archivo vacío. La garantía sigue viva en el build real.
export {};
